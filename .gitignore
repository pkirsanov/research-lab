# Option-chain snapshots are generated by scripts/fetch-options.mjs during the
# Pages deploy and served from the Pages artifact — they are NOT committed
# (keeps the repo free of recurring multi-MB data churn).
/data/options/
