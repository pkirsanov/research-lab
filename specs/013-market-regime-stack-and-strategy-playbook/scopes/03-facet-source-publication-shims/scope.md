# SCOPE-3: Facet source publication shims

**Status:** In Progress
**Tags:** `overlay:true`, `tier:1`
**Depends On:** SCOPE-2

## Objective

Turn each Tier 1 owner tool into a publication-only facet source. A shim reads its tool's already-computed frozen model output, maps it into a `RegimeFacetContract` reading through an explicit declared vocabulary mapping, stamps `asOf`, `sourceAttribution`, and `coverageNote`, and writes it into the Tier 0 shared-cache facet slot. A shim fetches nothing new and imports no Tier 2 module, so the dependency direction stays one-way and the no-cycle rule is provable.

## Implementation Files

| File | Change |
|---|---|
| `./rlratio.js` | Consumed as delivered by SCOPE-1 for the ratio-derived facet sources (credit-quality pulse, real-assets, global-rotation). |
| `./rlregime.js` | The `RegimeFacetContract` publish-side shape is consumed as delivered by SCOPE-2; this scope adds the no-cycle lint assertion path. |
| `./ratio-pairs.json` | Ratio pairs declared by a facet source are registered here alongside the SCOPE-1 entries. |
| `scripts/selftest.mjs` | Add the `regime-primitives` group covering facet publication shape and the IP-002 no-cycle assertion. |
| `sector-research-lab.html` | Publication shim only — breadth-participation + trend-structure facets from the existing sector model. |
| `market-heatmap-lab.html` | Publication shim only — breadth-participation facet over the existing constituent grid. |
| `bond-regime-lab.html` | Publication shim only — `credit`, `curve`, `duration-posture` as three separately identifiable facets, never blended. |
| `volatility-sizing-lab.html` | Publication shim only — strictly `kind: 'volatility-magnitude'` from the `rlvol.js` `regimeBand()` reading. |
| `gamma-trading-lab.html` | Publication shim only — volatility-magnitude + positioning-context facets. |
| `options-structure-lab.html` | Publication shim only — volatility-magnitude + positioning-context facets from call/put lean and unusualness values. |
| `real-assets-lab.html` | Publication shim only — `ratio-derived` facets over `RLRATIO` pairs carrying the proxy caveat. |
| `global-rotation-lab.html` | Publication shim only — `ratio-derived` international-pair facets, emitting `not-comparable`. |
| `trend-dynamics-cycle-lab.html` | Publication shim only — trend-structure facet via the declared versioned mapping. |

No path outside `design.md` → `## Implementation Boundary` is touched. Protected surfaces — including the `rldata.js` cache schema — are consumed through the existing append API and are not modified.

## Gherkin Scenarios

### BS-013-010: A facet source publishes its facet and never consumes the composed regime

```gherkin
Scenario: A facet source's model computation contains no read of the composed regime
  Given the breadth-participation facet source computes its own facet value
  When the facet source's model computation path is inspected
  Then it reads only its own inputs and publishes its facet value
  And it performs no read of the composed regime inside that computation
  And any such compute-side read is reported as a facet DAG cycle defect
```

## Consumer Impact Sweep

This scope relocates every Tier 1 facet-producing interface behind one published interface: a `RegimeFacetContract` reading written into the Tier 0 shared-cache facet slot. Each owner tool's locally named facet-shaped output stops being the surface a downstream reader binds to, so every producer of a facet value and every first-party reader of the shared per-tool read slot is a consumer of the contract published here. The enumeration below is the complete first-party consumer set bound by that contract; every path in it exists on disk today except `ratio-pairs.json`, which SCOPE-1 delivers. A stale-reference scan must return zero remaining first-party references that bind to a locally named facet-producing function instead of the published reading.

| Consumer surface | Path | What the published facet contract changes for it |
| --- | --- | --- |
| Sector facet source | `sector-research-lab.html` | Its existing sector model becomes the input to published breadth-participation and trend-structure facet readings; the in-page value stops being the bound interface. |
| Heatmap facet source | `market-heatmap-lab.html` | Its constituent-grid breadth measure is published as a breadth-participation facet reading rather than read in place. |
| Bond facet source | `bond-regime-lab.html` | `classifyCreditRegime` (line 1419), `classifyCurveState` (line 1455), and `classifyDurationPosture` (line 1709) each publish a separately identifiable `credit`, `curve`, and `duration-posture` facet, and none of the three is blended into a single score. |
| Volatility facet source | `volatility-sizing-lab.html`, `rlvol.js` `regimeBand()` (line 335) | Its magnitude-only band publishes strictly as `kind: 'volatility-magnitude'`, typed so the composer rejects it wherever a directional regime is expected. |
| Colliding band reader | `rlexperience-adapters/market-structure.js` `regimeBand(fg, trend, vix)` (line 1296) | Carries the same function name as `rlvol.js` `regimeBand()` while meaning something different (BP-4); it keeps deriving its own band and must not bind to the volatility-magnitude facet reading. |
| Options and gamma facet sources | `gamma-trading-lab.html`, `options-structure-lab.html`, `rlexperience-adapters/options.js` | The shared owner formulas producing the call/put lean and unusualness values become the input to published volatility-magnitude and positioning-context facet readings. |
| Real-assets facet source | `real-assets-lab.html` | Its ratio measures publish as `ratio-derived` facets over `RLRATIO` pairs, carrying the line-1170 directional-proxy caveat onto the published reading. |
| Global-rotation facet source | `global-rotation-lab.html` | Its international pair measures publish as `ratio-derived` facets and emit `not-comparable` where a pair fails the comparability predicate. |
| FX facet slot (declared; no host in this feature) | `rlfx.js` (`RLFX`) — **consumed, not modified** | No shim is authorized here. `rldata.js:309` already calls `RLFX.normalizeSourceEnvelope` (guarded at `:298`/`:312`), so a shared-cache write inside `rlfx.js` would close the `rldata.js → RLFX → rldata.js` cycle BP-1 forbids. The `fx` slot stays declared and composes as always-absent (`SOURCE_UNAVAILABLE`). |
| Trend-dynamics facet source | `trend-dynamics-cycle-lab.html` | Its existing vocabulary publishes as a trend-structure facet through an explicit declared versioned mapping rather than a silent re-label. |
| Tier 0 shared-cache slot API | `rldata.js` `putToolRead` / `toolRead` over the `toolReads` slot (lines 363, 445-461, 624) | The single write and read path every facet publication goes through; the cache schema is protected and stays unchanged. |
| Per-tool read overlay | `rlbrief.js` (line 1266), `market-brief.html` | Both read the owning tool's published per-tool read on each render, so the published facet reading is what they surface. |
| Headless coverage reader | `scripts/brief-refresh.mjs` `buildToolCoverage` (line 1173) | Reads the per-tool published reads and emits a per-tool `deepLink` for every registered tool id, so both the read and its deep link must keep resolving. |
| Payload validator | `scripts/validate-brief-payload.mjs` (lines 95-111) | Asserts every published per-tool read carries `read`, `metrics`, and `deepLink`, including its named `real-assets-lab` metrics assertion. |
| Distributed publisher | `scripts/brief-distributed-publish.mjs` (lines 172, 253) | Reads `snapshot.toolReads` and resolves a per-tool deep link from the registry, so a renamed read key breaks its deep link resolution. |
| Registry, navigation, and landing inventory | `tools.json`, `rlnav.js` `TOOLS` (line 45), `index.html` `TOOLS` (line 476) | The navigation entries and landing-page deep link targets keyed by tool id must continue to resolve unchanged for every registered facet-source owner tool. |
| Ratio pair registry | `./ratio-pairs.json` | Declares the pairs the ratio-derived facet sources read, alongside the SCOPE-1 entries. |
| Shared-cache contract selftest | `scripts/selftest.mjs` group `rldata.js — shared toolReads round-trip + freshness` (line 1536) | Pins the shared per-tool read round-trip and freshness contract that facet publication writes through, and must stay green byte-for-byte. |

Stale-reference scan surface: every navigation entry in `rlnav.js` `TOOLS` and `index.html` `TOOLS`, every landing-page deep link, every per-tool `deepLink` emitted by the headless readers, plus every remaining textual reference to a locally named facet-producing function across `*.html`, `*.js`, `*.mjs`, `notes/**`, and the registry JSON files.

## Implementation Plan

1. **Publication-only contract.** Each shim reads its owner tool's frozen model output, performs no new fetching, and writes exactly one `RegimeFacetContract` reading per facet it owns, stamped with `asOf`, `sourceAttribution`, and `coverageNote`.
2. **Explicit vocabulary mapping.** Every retained legacy vocabulary maps into the facet vocabulary through a declared, versioned mapping keyed by `valueVocabularyId` — lossless, or lossy with the loss declared. A silent re-label is a defect.
3. **Per-source facets.**
   - **sector** — breadth-participation and trend-structure facets from the existing sector model.
   - **heatmap** — breadth-participation facet over the constituent grid.
   - **bond** — three separately identifiable facets, `credit`, `curve`, and `duration-posture`, never blended into one score, because inflationary and disinflationary risk-off imply opposite bond consequences. Consumes `RLRATIO` for its credit-quality pulse pair.
   - **volatility** — publishes strictly `kind: 'volatility-magnitude'`, typed so the composer rejects it wherever a directional regime is expected.
   - **options / gamma** — volatility-magnitude and positioning-context facets.
   - **real-assets** — `ratio-derived` facets over `RLRATIO` pairs carrying the proxy caveat.
   - **global-rotation** — `ratio-derived` facets for international pairs, emitting `not-comparable` where the pair fails the comparability predicate.
   - **fx** — declared `ratio-derived` dollar facet slot with **no shim host in this feature**; no publication shim is built here, so the facet composes through the absent-facet path: `availability: 'unavailable'` with `SOURCE_UNAVAILABLE`, excluded from `m`, and named in `excludedFacetIds` / `absentFacetIds`. A future host publishes a `RegimeFacetContract` reading into this existing slot with zero change to the contract, the DAG, the composer, or the archetype registry. See design.md § Open Questions OQ-1 (resolved).
   - **trend-dynamics** — trend-structure facet with an explicit declared mapping from its existing vocabulary.
4. **No-cycle lint.** Add the IP-002 assertion that no facet source imports a Tier 2 module and that no facet declares the composed regime as an input. A fixture containing a source-side cycle resolves to an explicit lint-failure state rather than a value.
5. **Provenance.** Each published facet carries the producing source and the composer version it targets, so a downstream provenance line can name the producing facet source without ever naming the composed regime as a facet input.
6. **Shared-cache write path.** Writes go through the existing `rldata.js` append API into the Tier 0 facet slot. The cache schema itself is protected and is not changed.

### Test Plan

| Test ID | Test Type | Category | File/Location | Description | Command | Live System |
| --- | --- | --- | --- | --- | --- | --- |
| TP-03-01 | Functional | `functional` | `scripts/selftest.mjs` group `regime-primitives` / `facet sources publish exactly one RegimeFacetContract reading per owned facet and consume no composed regime` | **BS-013-010: A facet source publishes its facet and never consumes the composed regime** — each shim reads its owner tool's frozen model output, performs no new fetching, and writes exactly one `RegimeFacetContract` reading per owned facet stamped with `asOf`, `sourceAttribution`, and `coverageNote`. | `node scripts/selftest.mjs` | No |
| TP-03-02 | Unit | `unit` | `scripts/selftest.mjs` group `regime-primitives` / `every retained legacy vocabulary maps through a declared versioned valueVocabularyId mapping that is lossless or declared lossy` | Each retained legacy vocabulary maps into the facet vocabulary through a declared, versioned mapping keyed by `valueVocabularyId`; a mapping that is lossy declares its loss, and an undeclared silent re-label is rejected at the boundary. | `node scripts/selftest.mjs` | No |
| TP-03-03 | Unit | `unit` | `scripts/selftest.mjs` group `regime-primitives` / `bond publishes credit, curve, and duration-posture as three separately identifiable facets and volatility publishes strictly volatility-magnitude` | Per-source facet typing: the bond source publishes `credit`, `curve`, and `duration-posture` as three separately identifiable facets never blended into one score, and the volatility source publishes strictly `kind: 'volatility-magnitude'` so the composer rejects it wherever a directional regime is expected. | `node scripts/selftest.mjs` | No |
| TP-03-04 | Functional | `functional` | `scripts/selftest.mjs` group `regime-primitives` / `ratio-derived sources consume RLRATIO, propagate the proxy caveat, and emit not-comparable where the predicate fails` | The credit-quality-pulse, real-assets, and global-rotation sources read their readings from `RLRATIO` over pairs declared in `./ratio-pairs.json`, real-assets propagates the proxy caveat onto its published facet, and global-rotation emits `not-comparable` where a pair fails the comparability predicate. | `node scripts/selftest.mjs` | No |
| TP-03-05 | Integration | `integration` | `scripts/selftest.mjs` group `regime-primitives` / `facet writes go through the existing rldata.js append API into the Tier 0 facet slot with the cache schema unchanged and provenance stamped` | Publication reaches the Tier 0 shared-cache facet slot through the existing `rldata.js` append API with the protected cache schema unchanged, and each published facet carries the producing source and the targeted composer version so a provenance line can name the source without naming the composed regime as an input. | `node scripts/selftest.mjs` | No |
| TP-03-06 | Functional | `functional` | `scripts/selftest.mjs` group `regime-primitives` / `IP-002 no-cycle: no facet source imports a Tier 2 module and no facet declares the composed regime as an input` | **ADVERSARIAL RED-bite** — introduce a source-side cycle fixture in which a shim imports `./rlregime.js` and declares the composed regime as a facet input. The named test `IP-002 no-cycle: no facet source imports a Tier 2 module and no facet declares the composed regime as an input` MUST fail under that fixture, resolving to an explicit lint-failure state rather than a value, and MUST pass against the delivered one-way shims. | `node scripts/selftest.mjs` | No |
| TP-03-07 | Functional | `functional` | `scripts/selftest.mjs` — complete suite, every pre-existing group plus the additive `regime-primitives` group | Broad-suite regression: the full selftest suite stays green with the `regime-primitives` group added, every pre-existing group (including the SCOPE-1 and SCOPE-2 groups) is preserved byte-for-byte, and the total passing count does not decrease. | `node scripts/selftest.mjs` | No |
| TP-03-08 | Regression E2E | `e2e-ui` | `tests/market-regime-lab.spec.mjs` / `Regression: BS-013-010 every facet source publishes its facet and consumes no composed regime` | Persistent scenario-specific regression coverage for this scope's publication-only shims: a permanently registered case in the feature's real-page regression spec re-asserts that each Tier 1 owner tool publishes its `RegimeFacetContract` reading with `asOf` and `sourceAttribution`, and that no shim reads the composed regime. A re-introduced Tier 2 import inside a shim fails this named test by name. | `npx --no-install playwright test tests/market-regime-lab.spec.mjs --config=playwright.config.mjs --project=system-chrome --reporter=list` | Yes |
| TP-03-09 | Stress | `stress` | `scripts/selftest.mjs` group `regime-primitives-stress` / `the facet publication path sustains a repeated high-volume append run without unbounded slot growth or degraded write throughput` | Stress coverage for the shared-cache facet publication path: a repeated high-volume publication run across every registered facet source asserts that the Tier 0 facet slot append stays bounded, that repeated writes for the same source replace rather than accumulate, and that write throughput does not degrade across the run. | `node scripts/selftest.mjs` | No |
| TP-03-10 | Functional | `functional` | `scripts/selftest.mjs` group `regime-primitives` / `the declared fx facet slot with no shim host composes as unavailable with SOURCE_UNAVAILABLE and is excluded from m` | The declared `fx` slot has no shim host in this feature, so the fixture asserts `fx.dollar` composes as `availability: 'unavailable'` with `SOURCE_UNAVAILABLE`, is excluded from `m`, and is named in `excludedFacetIds` — never a neutral vote, never 0, and never a rendered FX value. | `node scripts/selftest.mjs` | No |

### Definition of Done

#### Core Items

- [x] `[TP-03-01]` `[BS-013-010]` A facet source's model computation contains no read of the composed regime: the breadth-participation shim reads only its own inputs and publishes its facet value as exactly one `RegimeFacetContract` reading per owned facet from its tool's frozen model output, stamped with `asOf`, `sourceAttribution`, and `coverageNote`, fetching nothing new, and any such compute-side read of the composed regime is reported as a facet DAG cycle defect. → Evidence: `facet sources publish exactly one RegimeFacetContract reading per owned facet and consume no composed regime` ✓. The harness extracts each page's SHIPPED `publishRegimeFacets`, evaluates it, and validates every reading through `RLREGIME.validateFacet` — it exercises the delivered code, not a reimplementation. Six sources publish 8 readings with unique `facetId`s, each carrying a non-empty `sourceAttribution` and `coverageNote`. A shim handed a null `asOf` publishes zero readings rather than stamping a fabricated time.
- [x] `[TP-03-02]` `[BS-013-010]` Every retained legacy vocabulary maps through a declared, versioned `valueVocabularyId` mapping that is lossless or declares its loss; an undeclared silent re-label is rejected. → Evidence: `every retained legacy vocabulary maps through a declared versioned valueVocabularyId mapping that is lossless or declared lossy` ✓. Every source declares both `valueVocabularyId` and `sourceVocabularyId` matching `/\/v\d+$/`; a non-injective map must set `lossy: true` with a non-empty `lossyFields`. The bond curve mapping is the one declared-lossy case: the source measures slope LEVEL (`Positive`) while the facet vocabulary's other members name slope CHANGE (`steepening`), so the loss is declared on the mapping rather than hidden by it.
- [x] `[TP-03-03]` `[BS-013-010]` The bond source publishes `credit`, `curve`, and `duration-posture` as three separately identifiable facets, and the volatility source publishes strictly `volatility-magnitude`. → Evidence: `bond publishes credit, curve, and duration-posture as three separately identifiable facets and volatility publishes strictly volatility-magnitude` ✓. Bond declares exactly 3 kinds across 3 distinct `facetId`s and never blends them into one score; volatility declares exactly 1 source of kind `volatility-magnitude`, which is directionless so the composer refuses it wherever a regime direction is expected.
- [ ] `[TP-03-04]` `[BS-013-010]` The credit-quality-pulse, real-assets, and global-rotation ratio-derived sources read through `RLRATIO`, propagate the proxy caveat, and emit `not-comparable` where the comparability predicate fails. → **HELD OPEN — the two ratio sources are not yet delivered.** `real-assets-lab.html` and `global-rotation-lab.html` classify by continuous 0-100 relative-strength scores and leader rankings, not by a closed vocabulary, and neither page loads `rlratio.js` today. Delivering this row means wiring `RLRATIO` into both pages and deriving the facet from ratio readings over declared pairs, which the Implementation Plan authorizes but which is more than the publication-only mapping the other six needed. Inventing score thresholds to manufacture a `leading`/`in-line`/`lagging` band would fabricate a reading those tools do not make. The gap is machine-enforced by `every declared facet source is either a delivered shim or an explicitly pending one`.
- [x] `[TP-03-10]` `[BS-013-010]` The declared `fx` slot has no shim host in this feature, so `fx.dollar` composes as `availability: 'unavailable'` with `SOURCE_UNAVAILABLE`, is excluded from `m`, and is named in `excludedFacetIds` — never a neutral vote, never 0, and never a rendered FX value. → Evidence: `the declared fx facet slot with no shim host composes as unavailable with SOURCE_UNAVAILABLE and is excluded from m` ✓. Composing a two-facet set where `fx.dollar` is unavailable gives `m === 1`, `absentFacetIds` containing `fx.dollar`, an exclusion reason of `SOURCE_UNAVAILABLE`, and no `fx.dollar` entry among the composed facets. The assertion also confirms `rlfx.js` contains no `putToolRead`, because that write would close the `rldata.js → RLFX → rldata.js` cycle BP-1 forbids.
- [x] `[TP-03-05]` `[BS-013-010]` Facet writes reach the Tier 0 slot through the existing `rldata.js` append API with the protected cache schema unchanged, and each facet carries its producing source and targeted composer version. → Evidence: `facet writes go through the existing rldata.js append API into the Tier 0 facet slot with the cache schema unchanged and provenance stamped` ✓. Facets ride inside the free-form `metrics` object: `putToolRead` validates an exact top-level key set, so a new top-level key would be REJECTED outright, making `metrics` the only seam that carries facets with the schema genuinely unchanged. The assertion pins that exact key list in `rldata.js` verbatim, so a schema edit fails here.
- [x] `[TP-03-09]` The facet publication path sustains a repeated high-volume publication run with the Tier 0 facet slot append staying bounded, repeated same-source writes replacing rather than accumulating, and no degraded write throughput across the run. → Evidence: `the facet publication path sustains a repeated high-volume append run without unbounded slot growth or degraded write throughput` ✓. 2000 publication rounds across every delivered shim leave the slot at exactly one entry per source and the reading count at exactly the declared facet count — a replace, not an accumulate — within the stated budget.
- [x] `[TP-03-06]` The adversarial source-side cycle fixture makes the named IP-002 no-cycle assertion fail before the delivered one-way shims and pass after them. → Evidence: RED-bite executed with the REAL cycle rather than a proxy. Adding `<script src="rlregime.js"></script>` to `volatility-sizing-lab.html` produced `✗ FAIL: IP-002 no-cycle: no facet source imports a Tier 2 module and no facet declares the composed regime as an input` and `2484 passed, 1 failed`, exit 1. Removing it restored `2485 passed, 0 failed`, exit 0, and `grep -c rlregime volatility-sizing-lab.html` returns `0`.
- [x] `[TP-03-07]` The complete selftest suite stays green with the additive `regime-primitives` group, every pre-existing group preserved and no decreased passing count. → Evidence: `node scripts/selftest.mjs` → `2486 passed, 0 failed`, exit 0, up from 2477 before this scope. No pre-existing group was edited; the SCOPE-1 and SCOPE-2 groups are preserved byte-for-byte.
- [x] `[BS-013-010]` A source whose reading cannot be published resolves to a typed `unavailable` / `not-comparable` state carrying a reason code and a what-would-resolve statement; no zero, dash, blank, or neutral value is published in its place. → Evidence: every shim routes an unmapped source value to `state: 'unavailable'` with a declared reason from its own `unavailableFor` table — `SOURCE_MIXED`, `SOURCE_INDETERMINATE`, `SOURCE_UNAVAILABLE` — and never coerces it into an in-vocabulary value. `mix` and `mixed` deliberately publish as unavailable rather than as a resolved middle value: a mixed gamma reading is an absence of positioning and a mixed trend direction is a failure to resolve, whereas `sideways` is a RESOLVED flat trend, and collapsing the two would let an unresolved read count as evidence in the confirmation denominator.
- [x] `[BS-013-010]` The shims carry no default value, no fallback path, and no stub: no shim imports a Tier 2 module, no shim declares the composed regime as a facet input, and no path outside `design.md` → `## Implementation Boundary` is modified. → Evidence: `IP-002 no-cycle: no facet source imports a Tier 2 module and no facet declares the composed regime as an input` ✓. No delivered page references `rlregime.js` or `RLREGIME`, and no source declaration mentions the composed regime. The same assertion pins all six mapper copies byte-for-byte after indentation normalization, so divergence between the per-page duplicates — unavoidable because the feature boundary allows no shared module — becomes a test failure.
- [x] `[BS-013-010]` Publishing the same frozen model output twice produces identical facet readings and an identical facet identity. → Evidence: `republishing a frozen model output yields identical readings and every shim numeric guard uses Number.isFinite` ✓. `JSON.stringify` equality holds across two `publishRegimeFacets` calls per shim on the same frozen input. Publication is pure given the model output and its as-of stamp: the shim reads no clock of its own, so republishing an unchanged read cannot move the facet identity.
- [x] `[BS-013-010]` Every numeric guard in new code uses `Number.isFinite`; the global `isFinite` appears zero times in the shim and selftest-group code added by this scope. → Evidence: the same assertion greps every extracted shim for a bare `isFinite(`, for `Date.now()` / `new Date()`, and for stub markers, requiring zero matches in all six. A bare `isFinite` coerces, so `isFinite(null)` is `true` and an absent as-of would pass a guard it should fail — which is precisely the check the shim's `Number.isFinite(Date.parse(asOf))` gate depends on.
- [ ] Consumer impact sweep is completed for every consumer surface enumerated in this scope's `## Consumer Impact Sweep` section, and zero stale first-party references remain to a locally named facet-producing interface across navigation entries, landing-page deep links, per-tool deep links, `*.html`, `*.js`, `*.mjs`, `notes/**`, and the registry JSON files. → **HELD OPEN — six of nine sources swept.** The delivered six publish through the enumerated `rldata.js` `putToolRead` path with their navigation entries and deep links unchanged. The sweep cannot be complete while `sector-research-lab.html`, `real-assets-lab.html`, and `global-rotation-lab.html` still expose only their locally named facet-producing interfaces.
- [ ] Scenario-specific E2E regression tests for every new/changed/fixed behavior in this scope are persistent and named — `[TP-03-08]` the feature's real-page regression spec holds a permanently registered case asserting that each Tier 1 owner tool publishes its `RegimeFacetContract` reading with `asOf` and `sourceAttribution` and that no shim reads the composed regime. → **HELD OPEN — blocked on SCOPE-4.** TP-03-08 targets `tests/market-regime-lab.spec.mjs` against `./market-regime-lab.html`; `ls` confirms neither exists. That page is SCOPE-4's deliverable, and building it here would breach this scope's publication-only Implementation Files table and pre-empt SCOPE-4's own scenarios.
- [ ] Broader E2E regression suite passes — the complete `node scripts/selftest.mjs` suite and the feature's real-page Playwright regression spec both run green once this scope lands, with every pre-existing selftest group and every previously registered regression case preserved and no decreased passing count. → **HELD OPEN — half met.** The selftest half is green at `2486 passed, 0 failed`, exit 0, with every pre-existing group preserved. The second half names the feature's real-page Playwright spec, which carries the same SCOPE-4 dependency as the item above.

#### Build Quality Gate

- [ ] Build Quality Gate — zero warnings across the scope's delivered surfaces; `artifact-lint.sh` clean for this spec directory; `traceability-guard.sh` clean for this spec directory; the broad `node scripts/selftest.mjs` suite green with no decreased count; no deferral language anywhere in the delivered artifacts; and `docs/` plus `notes/` synchronized with what this scope delivered. → **HELD OPEN — one clause unmet, and it is not this scope's.** Met: `node scripts/selftest.mjs` → `2486 passed, 0 failed`, exit 0; `node scripts/validate-trend-dynamics-cycle.mjs` → OK; zero warnings across the delivered shim surfaces. Unmet: `traceability-guard.sh` reports FAILED for the spec directory because scopes 04 and 06 are unbuilt and their mapped rows reference concrete test files that do not exist yet. The gate is written against the whole spec directory, so it cannot go clean until those scopes are built.
