# Scope 4 Execution Report — Anomaly Seeds And Red-Alert Candidacy Under The Gate

This file is the evidence surface for scope 4. It was created during planning as
a structural template and is filled from execution only. Nothing here may be
written from expectation, inference or summary. Every anchor below holds raw,
unfiltered terminal output with its exit code.

## Summary

Filled at execution.

## Test Evidence

### TP-04-01

Scenario SCN-020-014 — a finding with owner evidence and a certified-channel
transmission becomes a seed naming its channel.
Command: `node scripts/selftest.mjs`

### TP-04-02

Scenario SCN-020-014 — no frozen evidence bundle is required for the seed.
Command: `node scripts/selftest.mjs`

### TP-04-03

Scenario SCN-020-015 — seeds are clustered and assembled through the existing path
and scored by the owning module.
Command: `node scripts/selftest.mjs`

### TP-04-04

Scenario SCN-020-015 — a below-threshold candidate is rejected with a reason from
the closed class list, represented by counts rather than titles.
Command: `node scripts/selftest.mjs`

### TP-04-05

Scenario SCN-020-015 — every policy value is byte-identical by value, and a mutated
pass that lowers one is proven to admit a candidate that should have been rejected.
Command: `node scripts/selftest.mjs`

### TP-04-06

Scenario SCN-020-016 — the projection reports the existing publication gate
unchanged.
Command: `node scripts/selftest.mjs`

### TP-04-07

Scenario SCN-020-016 — any attempt to set published is refused; removing the
refusal is proven to render a fabricated live alert.
Command: `node scripts/selftest.mjs`

### TP-04-08

Scenario SCN-020-016 — the visible sentence says nothing went live while the
machine slug stays on a data attribute.
Command: `node scripts/selftest.mjs`

### TP-04-09

Scenario SCN-020-017 — a forbidden alarmist term is refused before any surface,
including inside a quoted thesis.
Command: `node scripts/selftest.mjs`

### TP-04-10

Scenario SCN-020-017 — the pre-check reads the owning module's frozen array rather
than a literal copy; the diff of that file is recorded.
Command: `node scripts/selftest.mjs`

### TP-04-11

Scenario SCN-020-018 — the empty statement is the exact committed copy and no weak
candidate is promoted.
Command: `node scripts/selftest.mjs`

### TP-04-12

Scenario SCN-020-024 — an unmappable transmission emits no seed and the no-channel
outcome is recorded by name.
Command: `node scripts/selftest.mjs`

### TP-04-13

Scenario SCN-020-024 — the closed channel vocabulary is at its committed membership
and an approximated channel is refused.
Command: `node scripts/selftest.mjs`

### TP-04-14

Scenario SCN-020-024 — the finding's eligibility for the action list and the
attention tier is unaffected.
Command: `node scripts/selftest.mjs`

### TP-04-15

Scenario SCN-020-025 — a seed with no frozen bundle assembles no candidate, stays
recorded, and the missing bundle is named.
Command: `node scripts/selftest.mjs`

### TP-04-16

Scenario SCN-020-025 — no bundle is substituted or synthesised; the acquisition
script is byte-identical.
Command: `node scripts/selftest.mjs`

### TP-04-17

Scenario SCN-020-015 — the market-action validator passes with topic-derived seeds
and candidates present and no committed value changed.
Command: `node scripts/validate-market-action.mjs`

## Build Quality Evidence

Filled at execution: unfiltered output and exit code for
`node scripts/selftest.mjs`, `node scripts/validate-market-action.mjs`,
`node scripts/validate-spec-test-paths.mjs`, and the verbatim
`git diff --name-only` output proving no excluded path was modified.

Also recorded here at execution: whether the forbidden-term list required an
additive export to be reachable for the pre-surface check, with the diff of that
file showing the added line and zero changed values — or the observation that no
export was required.

## Completion Statement

Filled at execution.
